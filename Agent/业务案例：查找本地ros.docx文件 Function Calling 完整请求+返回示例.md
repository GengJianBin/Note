# 业务案例：查找本地ros\.docx文件 Function Calling 完整请求\+返回示例

**业务场景**：用户提问「帮我查找本地名称为ros\.docx的文件的位置」

**前置说明**：客户端提前注册好**本地文件全局搜索工具**，模型无本地系统权限，只能通过标准化Function Calling指令，由客户端Agent运行时执行真实本地检索。

## 一、客户端发给大模型的完整请求Prompt（精简工程版）

以下为系统工具描述\+用户问题，完整送入LLM：

```plain
你是桌面端智能助手，可调用本地工具完成系统文件任务。
【可用工具列表】
工具名称: local_file_search
工具功能: 全局检索本地电脑文件，返回文件完整绝对路径
入参要求:
- fileName: string 必填，需要检索的完整文件名（含后缀）
返回值: 文件路径列表 / 未找到提示

【调用规则】
1. 如需调用工具，严格返回 JSON 结构化数据，禁止直接文本回答
2. 不需要工具则直接自然语言回答
3. 仅按照注册参数传参，不编造字段

【用户问题】
帮我查找本地名称为ros.docx的文件的位置
```

## 二、大模型返回的标准Function Calling调用指令（真实落地格式）

模型判断：该需求需要检索本地系统文件，无法文本回答，输出结构化调用指令：

```json
{
  "tool_name": "local_file_search",
  "parameters": {
    "fileName": "ros.docx"
  },
  "task_id": "agent_local_task_20260804_001"
}
```

## 三、客户端后续执行链路（面试必背串联）

1\. **运行时拦截校验**：Agent运行时解析JSON指令，校验工具合法、参数合规、无高危操作；

2\. **本地真实执行**：触发Electron主进程/Go引擎全局文件检索能力，遍历本地白名单目录查找文件；

3\. **结果回填上下文**：将检索结果（文件路径/未找到）规整后回传给大模型；

4\. **ReAct闭环收尾**：大模型基于结果生成自然语言答案，完成本轮任务，无需二次调用工具。

## 四、面试口述亮点（直接背）

以查找ros\.docx文件为例，我在客户端预先注册文件检索工具元数据，请求时携带工具规则与用户问题下发模型，模型不会直接回答，而是输出标准化文件检索调用指令，经过本地安全校验后执行真实系统检索，最后回填结果生成答案，完整走通了**用户需求→模型推理→工具调用→本地执行→结果回复**的客户端Function Calling全链路。

> （注：部分内容可能由 AI 生成）
